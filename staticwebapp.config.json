{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/runtime/",
            "backendRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}